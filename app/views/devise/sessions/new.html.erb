<h1 class="subheader">Prijavi se</h1>

<!-- Dve slike -->
<ul class="small-block-grid-2">
  <li>
    <blockquote>
      <div class="row">

      <%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
          <div class="form-inputs">
            <%= f.input :email, :required => false, :autofocus => true %>
            <%= f.input :password, :required => false %>
            <%= f.input :remember_me,  :as => :boolean if devise_mapping.rememberable? %>
          </div>

          <div class="form-actions">
            <%= f.button :submit, "Prijavi se" %>
          </div>
         </div>
          <% end %>
          <cite>Registruj se na portlal "Ja Fizika" i omogući sebi besplatan pristup svim testovima sa takmičenja iz fizike namenjenim učenicima osnovnih škola !</cite>
          <%= render "devise/shared/links" %>

      </blockquote>
  </li>
  <li><%= image_tag("/image/cikicaok.png" , :class => "bevel" , :size => "300x500" ) %></li>
</ul>
 <!-- -->
